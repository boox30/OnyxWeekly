<%= form_for(@weekly) do |f| %>
  <% if @weekly.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weekly.errors.count, "error") %> prohibited this weekly from being saved:</h2>

      <ul>
      <% @weekly.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_area :body, :class => "form-control", :rows => "20", :cols => "40"%>
  </div>
  <div class="actions">
    <%= f.submit "Save", :class => "btn btn-primary" %>
    <%= link_to "back", root_path,  :method => "get", :class => 'btn btn-default'%>
  </div>
<% end %>
